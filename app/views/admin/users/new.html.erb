<div class="page">
  <div class="pageHead">
    <h1 class="pageHead__title">スタッフ新規作成</h1>
    <div class="pageHead__actions">
      <%= link_to "一覧へ", admin_users_path, class: "btn btn--ghost btn--sm" %>
    </div>
  </div>

  <%= render "form", user: @user %>
</div>
